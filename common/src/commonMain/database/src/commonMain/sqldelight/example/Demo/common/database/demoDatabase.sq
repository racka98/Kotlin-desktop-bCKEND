CREATE TABLE IF NOT EXISTS DemoEntity (
    id TEXT,
    name TEXT,
    password INTEGER NOT NULL,
    identification TEXT NOT NULL ,
    data BLOB
);

selectAll:
SELECT *
FROM DemoEntity;

insertReminder:
INSERT OR IGNORE INTO DemoEntity(id, name,password,identification,data)
VALUES (?,?,?,?,?);